{
  "name": "ui5dev",
  "version": "0.0.1",
  "description": "UI5 apps dev tool",
  "bin": {
    "ui5dev": "dist/bin/ui5dev.js"
  },
  "scripts": {
    "build": "babel src -d dist",
    "build-watch": "npm run build -- --watch",
    "test": "echo \"Error: no test specified\""
  },
  "author": "",
  "license": "MIT",
  "preferGlobal": true,
  "dependencies": {
    "babel-preset-env": "^1.3.3",
    "chalk": "^1.1.3",
    "chokidar": "^1.6.1",
    "commander": "^2.9.0",
    "express": "^4.15.2",
    "express-http-proxy": "^1.0.4",
    "morgan": "^1.8.1",
    "output-file-sync": "^2.0.0",
    "rimraf": "^2.6.1",
    "rollup": "^0.41.6",
    "rollup-plugin-babel": "^2.7.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": 6.5
          }
        }
      ]
    ]
  }
}
